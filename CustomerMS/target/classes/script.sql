CREATE DATABASE IF NOT EXISTS CustomerMSDB;
USE CustomerMSDB;

DROP TABLE IF EXISTS IC_CUSTOMER;

CREATE TABLE IF NOT EXISTS IC_CUSTOMER(
	EMAIL_ID varchar(45) PRIMARY KEY,
    NAME varchar(80) NOT NULL,
    PASSWORD varchar(80) NOT NULL,
    PHONE_NUMBER varchar(10) NOT NULL UNIQUE
);


INSERT INTO IC_CUSTOMER (EMAIL_ID, NAME, PASSWORD, PHONE_NUMBER) VALUES ("john@gmail.com", "John", "johnpassword", "1234567890");
INSERT INTO IC_CUSTOMER (EMAIL_ID, NAME, PASSWORD, PHONE_NUMBER) VALUES ("peter@gmail.com", "Peter", "peterpassword", "1234567891");
INSERT INTO IC_CUSTOMER (EMAIL_ID, NAME, PASSWORD, PHONE_NUMBER) VALUES ("test@gmail.com", "Test", "TestPassword", "1234567892");

commit;
